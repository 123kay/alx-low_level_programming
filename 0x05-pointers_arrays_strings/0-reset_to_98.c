#include <main.h>
#include <stdio.h>

int main (void)
{
 char ch;
 int n;

 ch = n

    n = 402;
    printf("n=%d\n", n);
    reset_to_98(*n);
    putchar("ch");
    printf("n=%d\n", *n);
    return (0);
}
